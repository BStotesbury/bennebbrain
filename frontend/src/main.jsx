import React from 'react'
import ReactDOM from 'react-dom/client'
import App from './App'
import './index.css'   // <-- this must import the Tailwind + the layers above

ReactDOM.createRoot(document.getElementById('root')).render(<App />)